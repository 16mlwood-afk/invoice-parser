{
  "name": "amazon-invoice-parser",
  "version": "1.1.0",
  "description": "A Node.js utility to extract structured data from Amazon invoice PDFs. Companion tool for the Amazon Invoice Extractor Chrome extension.",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "jest --config config/jest.config.js --testPathPattern=unit",
    "test:unit": "jest --config config/jest.config.js --testPathPattern=unit",
    "test:integration": "jest --config config/jest.config.js --testPathPattern=integration",
    "test:e2e": "jest --config config/jest.config.js --testPathPattern=e2e",
    "test:all": "jest --config config/jest.config.js",
    "test:watch": "jest --config config/jest.config.js --testPathPattern=unit --watch",
    "test:coverage": "jest --config config/jest.config.js --testPathPattern=unit --coverage",
    "test:integration:coverage": "jest --config config/jest.config.js --testPathPattern=integration --coverage",
    "test:e2e:coverage": "jest --config config/jest.config.js --testPathPattern=e2e --coverage",
    "test:e2e:report": "mkdir -p tests/e2e/reports && jest --config config/jest.config.js --testPathPattern=e2e --json --outputFile=tests/e2e/reports/latest-run.json && node tests/e2e/generate-report.js",
    "test:pdfs": "node scripts/testing/test-real-pdfs.js",
    "start": "node index.js",
    "start:api": "node index.js --api",
    "start:web": "node index.js --web",
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "web:start": "cd web && npm run start",
    "web:test": "cd web && npm test",
    "web:test:watch": "cd web && npm run test:watch",
    "web:test:coverage": "cd web && npm run test:coverage",
    "web:lint": "cd web && npm run lint",
    "web:lint:fix": "cd web && npm run lint:fix",
    "web:install": "cd web && npm install",
    "lint": "eslint index.js scripts/*.js",
    "build": "echo 'No build process required'",
    "clean": "rm -rf coverage/ coverage-e2e/ tests/e2e/reports/ web/coverage/",
    "bmad:refresh": "echo 'Use: bmad install -f -i opencode'",
    "bmad:list": "echo 'Use: bmad status'",
    "bmad:validate": "echo 'Use: bmad status && echo Configuration validated'"
  },
  "keywords": [
    "amazon",
    "invoice",
    "parser",
    "pdf",
    "extractor",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "bmad-method": "^4.44.3",
    "commander": "^11.1.0",
    "cors": "^2.8.5",
    "csv-stringify": "^6.5.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "joi": "^17.9.2",
    "multer": "^2.0.2",
    "pdf-lib": "^1.17.1",
    "pdf-parse": "^1.1.1",
    "pdf2pic": "^3.2.0",
    "winston": "^3.8.2"
  },
  "directories": {
    "doc": "docs"
  },
  "devDependencies": {
    "@babel/core": "^7.28.5",
    "@babel/preset-env": "^7.28.5",
    "eslint": "^8.57.0",
    "jest": "^29.6.1",
    "supertest": "^7.1.4"
  }
}
